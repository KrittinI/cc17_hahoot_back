const { Router } = require('express')
// const eventController = require('../controllers/event-controller')

const eventRouter = Router()

// eventRouter.get('/',eventController)

module.exports = eventRouter